name: SOLRAK v0.1.94 Windows hardware native (superseded)

# La validación autoritativa de v0.1.94 es:
# .github/workflows/solrak-v0194-hardware-native-safe.yml
# Este archivo queda sin ejecución automática para conservar trazabilidad de runs históricos.

on:
  workflow_dispatch:

jobs:
  superseded:
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: echo "Superseded by solrak-v0194-hardware-native-safe.yml"
